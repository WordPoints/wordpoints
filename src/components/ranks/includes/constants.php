<?php

/**
 * Constants and global variables of the ranks component.
 *
 * @package WordPoints\Ranks
 * @since 1.7.0
 */

global $wpdb;

$wpdb->wordpoints_ranks      = "{$wpdb->base_prefix}wordpoints_ranks";
$wpdb->wordpoints_rankmeta   = "{$wpdb->base_prefix}wordpoints_rankmeta";
$wpdb->wordpoints_user_ranks = "{$wpdb->base_prefix}wordpoints_user_ranks";

// EOF
